@command
def test(app):
    # run("sh --posix uninstall.sh")
    # run("sh --posix uninstall.sh")
    # run("sh --posix install.sh")
    # run("sh --posix install.sh")

    check_program("dash")

    run("dash uninstall.sh")
    run("dash uninstall.sh")
    run("dash install.sh")
    run("dash install.sh")

@command
def debug(app, shell="dash"):
    check_program(shell)
    run(f"{shell} -vx install.sh")
